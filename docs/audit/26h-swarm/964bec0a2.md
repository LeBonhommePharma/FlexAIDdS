# Audit: 964bec0a2 — Merge pull request #258 bonhomme-fleet-dataset-runner

## Summary (2-4 sentences)
Merge commit `964bec0a2bb5ea6518868a9487288d0ddb81ac5c` lands **zero tree changes** relative to first parent `155ebbb7b` (dependabot checkout bump). GitHub PR #258 reports `additions: 0`, `deletions: 0`, `changed_files: 0`; trees of `155ebbb7b`, second parent `a4e13fc9b`, and `964bec0a2` are **byte-identical** (`f6eb92abfb952b67d09b67073c345d7c3f980542`). The only unique non-merge commit on the branch tip path, `4beb3b36d`, **destroyed** root `CMakeLists.txt` (3226 lines → a 78-byte placeholder comment referencing “full content from Grok's previous”) and was fully discarded when `master` was merged into `feature/bonhomme-fleet-dataset-runner` (`a4e13fc9b`). Branch/PR naming promises Bonhomme Fleet DatasetRunner work; the actual fleet integration landed one minute later as **PR #257** (`8eaf043ae`, 3832 files), not this PR.

## Severity: HIGH (process / history hygiene) · LOW (live tree / science)

Tree impact of the merge result is nil. Process and archaeology impact is high: empty PR merged with empty template body, no human reviews, many red CI jobs, and a permanently retained non-buildable intermediate commit on the second-parent DAG.

## Merge topology (verified)

| Ref | Role | Notes |
|-----|------|--------|
| `964bec0a2` | Merge PR #258 → `master` | Parents: `155ebbb7b` + `a4e13fc9b`; result tree = master |
| `155ebbb7b` | First parent (`master` tip) | Merge PR #255 `actions/checkout` 7.0.0 |
| `a4e13fc9b` | Second parent (branch tip) | “Merge branch 'master' into feature/bonhomme-fleet-dataset-runner”; tree = master |
| `4beb3b36d` | Sole unique content commit | CMake wipe; **not** an ancestor of first parent; **is** ancestor of merge via second parent |
| Parent of wipe | `d8a422f93` | “Chore: ignore .tools/ and .venv*/…” |

Commands used:
```bash
git rev-parse 155ebbb7b^{tree} a4e13fc9b^{tree} 964bec0a2^{tree}
# → all f6eb92abfb952b67d09b67073c345d7c3f980542
git diff --name-status 964bec0a2^1 964bec0a2   # empty
git show 4beb3b36d:CMakeLists.txt
# → "# New modular top-level CMakeLists.txt ... (full content from Grok's previous)"
```

## Findings

### F1. Empty merge — no files land on `master` (INFO · tree / CRITICAL · mislabel)
- Evidence: GitHub API `changed_files: 0`, `additions: 0`, `deletions: 0`. `git diff-tree -r 964bec0a2` empty. Triple tree equality above.
- Why it matters: First-parent history now contains a merge titled for fleet DatasetRunner / CMake modularization that **does nothing**. `git log --first-parent` archaeology will attribute fleet/CMake work to this SHA incorrectly. Violates “inspect first, claim never” if anyone cites PR #258 as “fleet landed.”
- Fix recommendation: Treat #258 as a no-op in any release notes. Prefer PR #257 / commits `69aa0fab6`, `2fc7189d8`, later `d842e3247` / `413141f07` for fleet. Optional: document in release hygiene that empty merges should be closed not merged.

### F2. Intermediate commit is a full CMakeLists catastrophe (HIGH if checked out · MEDIUM in history)
- Evidence: `4beb3b36d` stats `+1 / −3226` on `CMakeLists.txt` only. Blob `b27f97fc4…` is 78 bytes of prose, not CMake. No modular replacement modules added in that commit (`git diff --name-status d8a422f93 4beb3b36d` → only `CMakeLists.txt`). Message claims “modular targets, zero monolith, PGO-first, Eigen3-centric, hardware acceleration paradise” with **no implementation**.
- Why it matters: Any `git checkout 4beb3b36d`, bisect landing on that commit, or rebase/cherry-pick of the commit alone yields a tree that cannot configure (`cmake` will fail immediately). Placeholder text explicitly defers to “Grok's previous” content that was never committed — classic incomplete agent handoff left in the permanent object store.
- Fix recommendation: Never cherry-pick `4beb3b36d`. Do not restore it via revert of the master-into-branch merge. If modular top-level collapse is still desired, implement it as a **complete** PR: real `CMakeLists.txt` + existing `cmake/FlexAID{Components,Dependencies,Helpers,Options}.cmake` wiring, with configure+build+`ctest` gates (AGENTS.md). Do **not** force-rewrite history without explicit human approval (AGENTS unsafe-git rule).

### F3. Master merge into branch silently discarded the only “feature” (MEDIUM · process)
- Evidence: `a4e13fc9b` is merge of `4beb3b36d` + `155ebbb7b`. Diff `4beb3b36d..a4e13fc9b` restores `CMakeLists.txt` (+2948 lines). Result tree equals master, so the PR became mergeable as a pure no-op.
- Why it matters: GitHub “Merge” UI can show a green/mergeable PR after updating branch even when the **intended** change was rejected by conflict resolution (theirs/master wins on the only modified path). No conflict markers remain in the merge commit message; no reviewer note explains “intentional discard.”
- Fix recommendation: PR template should require a non-empty `git diff base...head` summary. CI job: fail if `git diff --stat origin/master...HEAD` is empty (or require `allow-empty-merge` label). Agents must not open/merge PRs whose only content was wiped by update-branch.

### F4. Name/title/body mismatch — fleet branding on a CMake stub PR (MEDIUM · hygiene)
- Evidence:
  - Branch: `feature/bonhomme-fleet-dataset-runner`
  - PR title: `CMake: entropy collapse — modular targets, zero monolith, PGO-first, …`
  - PR body: stock template; **all** scope/validation/security/reproducibility boxes unchecked; Summary is placeholder “What changed and why.”
  - Real fleet DatasetRunner work: PR **#257** `feature/bonhomme-fleet-dataset-runner-v1` (`8eaf043ae`), merged ~1 min later, +115842 / −36074 across 3832 files; also later INDEX items `69aa0fab6`, `2fc7189d8`, `d842e3247`, `413141f07`.
- Why it matters: Swarm indexes, `git log --grep=fleet`, and human memory will attach “fleet DatasetRunner” to a no-op SHA. Confuses 26h audit swarm and any claim of “fleet backend landed in #258.”
- Fix recommendation: Close empty misnamed PRs; rename branches before open; require PR body to match `git diff --stat`. Cross-link #257 as the real fleet land for this naming lineage.

### F5. Merged with many failing required-looking CI checks and zero reviews (HIGH · process / AGENTS.md)
- Evidence (`gh pr checks 258` sample):
  - **fail**: C++ core linux-clang, linux-gcc, linux-gcc-asan, linux-gcc-mpi (duplicate runs); Coverage; ASan/UBSan; Pure Python results; Python bindings smoke (windows); tsan-linux
  - **pass**: some macos-clang, linux-gcc-avx512, license scan, hygiene, skill dry-run, CodeQL analyze, CodeRabbit
  - Reviews: **0** (`gh api .../pulls/258/reviews` empty)
- Why it matters: AGENTS.md requires verify-before-done and zero test failures before push. Merging a PR with red C++/Python jobs (even if tree-identical to master) normalizes bypassing gates. If branch protection allowed this empty merge, protection is weak for real fleet PRs too.
- Fix recommendation: Enforce branch protection: required green C++ matrix + pure Python + no merge with failing checks; require ≥1 review or CODEOWNERS; block empty diffs. Note: failures may partly be base `master` red at the time (this merge introduced no new code) — still a governance failure to merge without attestation.

### F6. No ranking, scoring, GA budget, or thermodynamic path change (INFO)
- Evidence: Empty result diff. No touch of `LIB/gaboom.cpp`, `Vcontacts`, `BindingMode`, `statmech`, DatasetRunner election, FO MinPts, or claim scripts in the **landed** tree delta (there is none).
- Why it matters: Science/repro of docking scores is unaffected by this merge result. Do not attribute score/ranking shifts to `964bec0a2`.
- Fix recommendation: None for science. Attribute fleet/DatasetRunner behavioral changes to #257 and subsequent fleet commits only.

### F7. “Entropy collapse” messaging vs CF/thermo contract (LOW · language)
- Evidence: Commit subject uses “entropy collapse” for a **build-system** rewrite that never shipped. AGENTS.md forbids loose ΔG/entropy claims; here the risk is only confusing “entropy” engineering branding with thermodynamic ledger work.
- Why it matters: Search hits for “entropy collapse” already mix musical/site commits (`723c81480`) and docking entropy eval (`d20cd5529`). Adding a false CMake “entropy collapse” no-op worsens noise.
- Fix recommendation: Prefer “CMake modularization” wording for build PRs; reserve entropy language for StatMech / Shannon / tENCoM paths.

### F8. Modular CMake scaffolding may already exist elsewhere — not via this PR (INFO)
- Evidence: Current tree has `cmake/FlexAIDComponents.cmake`, `FlexAIDDependencies.cmake`, `FlexAIDHelpers.cmake`, `FlexAIDOptions.cmake` (~589 lines total) and a full root `CMakeLists.txt` (~2800+ lines depending on tip). None of that was delivered by `4beb3b36d` or `964bec0a2`.
- Why it matters: Future agents might assume modular collapse landed in #258; it did not. Root remains largely monolithic unless later commits actually `include()` the helper modules.
- Fix recommendation: Separate audit/PR for whether root `CMakeLists.txt` fully delegates to `cmake/FlexAID*.cmake` (out of scope here).

### F9. Security (INFO)
- Evidence: No new network surface, secrets, path injection, or dependency changes in the merge result (empty). Placeholder commit has no executable content.
- Fix recommendation: N/A for landed tree.

### F10. Reproducibility of the PR event itself (MEDIUM · meta)
- Evidence: Merge timestamp 2026-07-14 20:37:31 -0400; update-branch merge `a4e13fc9b` at 20:37:20; PR #257 fleet land 20:38:31. Two fleet-named PRs within ~70s, one empty, one huge — looks like parallel agent/user merge thrash.
- Why it matters: Hard to reconstruct intent; empty merge may have been accidental “merge because green merge button after update branch.”
- Fix recommendation: After multi-agent bursts, run `git log --first-parent --merges -20` and flag empty merges. Prefer one fleet PR series with sequential review.

## Ranking/scoring impact: NO

Landed tree is identical to pre-merge `master`. No CF scoring, GA, clustering, election, or claim-metric code changed by this merge.

## Reproducibility impact: NO (code) · YES (negative process)

- **Code/artifacts:** No change to binaries, configs, or benchmarks from the merge result.
- **Process/history:** Negative — empty merge + wiped intermediate commit pollute history and invite false attribution of fleet/CMake work to `964bec0a2` / #258.

## Tests adequate: N/A (no code) · NO (merge process)

No tests were added or required for a no-op. Merge proceeded despite multiple failing CI jobs and zero reviews — process tests/gates inadequate.

## Verdict: NO_OP_ACCEPT · PROCESS_FAIL

**Keep** the merge on `master` (reverting it is pointless noise; tree already matches parent). **Do not** treat PR #258 as delivering Bonhomme Fleet or CMake modularization. **Never** resurrect `4beb3b36d`. For fleet DatasetRunner reality, audit **PR #257** (`8eaf043ae`) and commits `69aa0fab6` / `2fc7189d8` / later fleet production work. Hardening follow-ups (branch protection against empty diffs + red CI, PR body/diff consistency checks) are process fixes outside this commit’s tree.

## Evidence appendix

```text
PR #258
  title: CMake: entropy collapse — modular targets, zero monolith, PGO-first, …
  head:  feature/bonhomme-fleet-dataset-runner
  base:  master
  merge_commit_sha: 964bec0a2bb5ea6518868a9487288d0ddb81ac5c
  commits: 4beb3b36d, a4e13fc9b
  files: []  additions: 0  deletions: 0

4beb3b36d CMakeLists.txt full content:
  "# New modular top-level CMakeLists.txt ... (full content from Grok's previous)"

PR #257 (real fleet land, ~1 min later)
  head: feature/bonhomme-fleet-dataset-runner-v1
  merge: 8eaf043ae
  changedFiles: 3832  additions: 115842  deletions: 36074
```
