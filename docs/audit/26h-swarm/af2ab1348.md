# Audit: af2ab1348 — Merge PR #270 docs/v2.0.3-install-metal-notes

| Field | Value |
|-------|--------|
| **Short** | `af2ab1348` |
| **Full SHA** | `af2ab13484252eeb8ad5a3eccba2fb40fabe4a26` |
| **Subject** | Merge pull request #270 from LeBonhommePharma/docs/v2.0.3-install-metal-notes |
| **PR** | [#270](https://github.com/LeBonhommePharma/FlexAIDdS/pull/270) — *Docs: INSTALLATION v2.0.3 + stable --with-metal* |
| **Author / Committer** | Louis-Philippe Morency (LeBonhommePharma) / GitHub merge bot |
| **AuthorDate / CommitDate** | 2026-07-15 00:55:29 −0400 |
| **First parent (`^1`, main)** | `342d6650d` — Merge PR #268 (formula sha256 for v2.0.3) |
| **Second parent (`^2`, PR tip)** | `4d51e413e` — Revert "Fix: Harden self-hosted Metal gate + exact release language" |
| **Scope (net vs `^1`)** | **`docs/INSTALLATION.md` only** (+21 / −8) |
| **Audit type** | Docs-only install merge audit (read-only); **no source edits** |
| **Verdict** | **DOCS_ONLY / MERGE_OK** |

## Summary (2–4 sentences)

Merge `af2ab1348` lands PR **#270**, which aligns `docs/INSTALLATION.md` with the already-shipped **v2.0.3** packaging surface: stable version notes **2.0.2 → 2.0.3**, formula `head` branch **`master` → `main`**, and Homebrew Metal install as **`brew install --build-from-source --with-metal …` on stable** (no longer requiring `--HEAD`). The net tree change vs main is a single markdown file; intermediate PR commits that touched CI Metal-gate docs/workflow were **added then fully reverted**, so they do not land. Technical claims in the doc match formula / CMake / VERSION state at this merge (PR **#260** flexaid_core Metal link ancestry + tag `v2.0.3`). Residual risk is documentation language boundary only: “clean Metal **link**” is true for packaging, not a substitute for self-hosted Metal release validation (`docs/SUPPORT_MATRIX.md` still marks Metal **Experimental**).

## Severity: LOW

Docs hygiene / operator-facing install accuracy only. No engine, scoring, ranking, GA budget, CMake, formula, or CI files change in the **net** merge.

---

## Merge topology

```text
342d6650d  Merge PR #268 (v2.0.3 formula sha)     ← first parent (main)
     │
80f6ba783  Docs: Align INSTALLATION with v2.0.3 stable Metal Homebrew path
e752df7aa  Fix: Harden self-hosted Metal gate + exact release language   (transient)
4d51e413e  Revert e752df7aa                                               ← second parent
     │
     └──────────────────────────► af2ab1348  Merge PR #270 ◄──┘
```

| Check | Result |
|-------|--------|
| Parent count | 2 (true GitHub merge) |
| Commits on `^2` not in `^1` | **3** — `80f6ba783`, `e752df7aa`, `4d51e413e` |
| Net files vs `^1` | **1** — `docs/INSTALLATION.md` |
| `git diff 342d6650d 4d51e413e --name-only` | Only `docs/INSTALLATION.md` (CI revert cancels harden) |
| Tree: CI files equal `^1` vs `^2` | Yes (`metal-self-hosted.yml`, `CI_RELEASE_GATES.md` blob IDs match first parent) |
| Merge tree ≡ second parent for INSTALLATION | Yes (`4509d5333…`) |
| Conflict markers | None |
| `LIB/` / formula / CMake / tests | **None** |

**Interpretation:** The PR branch briefly staged a large Metal release-gate hardening commit (`e752df7aa`, +192/−18 on workflow + `CI_RELEASE_GATES.md`), then immediately reverted it (`4d51e413e`) so the branch tip matched “docs only.” GitHub’s PR files API correctly reports only `docs/INSTALLATION.md`. Process smell (noise on branch), not a product defect in the landed tree.

---

## What landed (net content)

### Operator-facing deltas in `docs/INSTALLATION.md`

| Topic | Before (`^1`) | After (merge) |
|:--|:--|:--|
| Stable version label | v2.0.2+ | **v2.0.3+** |
| Version alignment list | formula ↔ pyproject ↔ `__version__.py` | + **`VERSION.md`**; all at **2.0.3** |
| Formula head branch | “tracks `master`” (release notes bullet) | **`main`** |
| Metal Homebrew | Not shown as stable path (stale vs formula after #267/#268) | **`brew install --build-from-source --with-metal lebonhommepharma/flexaidds/flexaidds`** in three sections |
| Metal rationale | (missing / outdated) | Stable v2.0.3+ links bridges via **flexaid_core** (PR **#260**); **no longer requires `--HEAD`** for a clean Metal **link** |
| Metal reinstall | — | Comment: `brew reinstall --build-from-source --with-metal …` after tap update |
| Maintainer TAG example | `TAG=v2.0.2` | **`TAG=v2.0.3`** |
| Source Metal section | cmake only; “Requires: macOS with Xcode” | cmake + Homebrew stable path; Xcode + **Metal toolchain** note (`xcodebuild -downloadComponent MetalToolchain`); **≥12 GiB free disk** guidance |

No other paths change.

---

## Findings

### F1. Net merge is pure documentation (INFO / positive)

- Evidence: `git diff --name-status 342d6650d af2ab1348` → single `M docs/INSTALLATION.md` (+21/−8). PR #270 GitHub `files` array matches. No `LIB/`, `Formula/`, `CMakeLists.txt`, workflows, Python package code, or tests.
- Why it matters: Ranking, CF/contact-function scoring proxy, StatMech, GA budgets, and binaries are byte-identical to first parent for executable behavior.
- Fix recommendation: None.

### F2. Version / head / Metal-path claims are factually correct at this SHA (INFO / positive)

Verified against tree at `af2ab1348` (same as main after #268):

| Claim in INSTALLATION | Evidence |
|:--|:--|
| Versions aligned at **2.0.3** | `python/pyproject.toml` `version = "2.0.3"`; `python/flexaidds/__version__.py` `__version__ = "2.0.3"`; `VERSION.md` Current Version **2.0.3**; formula `url` …`/tags/v2.0.3.tar.gz` |
| Tag exists | Annotated tag `v2.0.3` → `388e17336` (release merge #267); sha fixed in #268 |
| `head` tracks **main** | `Formula/flexaidds.rb`: `head "…FlexAIDdS.git", branch: "main"` |
| Stable `--with-metal` allowed | Formula `option "with-metal"`; **no** `odie` refusing stable Metal (removed in #267 after #260) |
| flexaid_core Metal link (PR #260) | `CMakeLists.txt` attaches `LIB/metal_eval.mm`, `MetalRMSDBridge.mm`, Shannon/tENCoM/Cavity/TurboQuant Metal bridges to `flexaid_core` with **PUBLIC** Metal/Foundation/MetalKit; comments state Homebrew multi-target LTO rationale |
| Install command shape | Matches formula caveats: `brew install --build-from-source --with-metal lebonhommepharma/flexaidds/flexaidds` and reinstall variant |

This merge **closes** the INSTALLATION lag noted in the v2.0.3 packaging audit (`388e17336` F4): install docs no longer advertise 2.0.2 / master-era Metal HEAD-only path.

### F3. Language is “clean Metal **link**,” not production Metal validation (LOW)

- Evidence:
  - INSTALLATION: “Stable v2.0.3+ links bridges via flexaid_core (PR #260) — no longer requires `--HEAD` for a **clean Metal link**.”
  - Unchanged at this merge: `docs/SUPPORT_MATRIX.md` — Metal = **Experimental**; hosted CI = shader smoke only; full link/runtime = self-hosted `self-hosted-m3`.
  - Unchanged: `docs/CI_RELEASE_GATES.md` — soft-skip of hosted smoke does **not** authorize Metal release claims; full gate is `metal-self-hosted.yml`.
- Why it matters: Operators may read “stable Metal” as “Metal GPU docking is CI-proven.” The doc carefully says **link**, and points at PR #260’s packaging fix—not Astex throughput or GPU numerical parity. Still, three repeated brew Metal install blocks without a one-line “Metal remains experimental (see SUPPORT_MATRIX)” could oversell if skimmed.
- Fix recommendation (optional follow-up, **not** blocking this merge): One cross-link under the Homebrew Metal snippet to `docs/SUPPORT_MATRIX.md` / `docs/CI_RELEASE_GATES.md` (“link fix ≠ self-hosted full gate”).

### F4. Branch noise: harden-then-revert of Metal CI docs (LOW — process)

- Evidence:
  - `e752df7aa` (+192/−18): expands `metal-self-hosted.yml` (dual binary, nm symbol smoke, fail-closed Metal-OFF) and adds allowed/forbidden claim tables + LP checklist in `CI_RELEASE_GATES.md`.
  - `4d51e413e`: exact reverse of that commit.
  - Net vs main: zero bytes on those paths (blob IDs equal first parent).
- Why it matters: `git log` / blame history of the PR is misleading if someone audits “commits on the branch” without tree diff; later PR **#271** (`8c42517bd` / merge `125f76cc8`) re-lands similar hardening properly. This merge did **not** silently drop or ship those CI changes—revert is complete.
- Fix recommendation: Prefer separate PRs for docs vs CI; avoid commit-then-revert on the same PR tip before merge. No action required on `main` for this SHA.

### F5. Residual doc drift outside INSTALLATION (LOW / residual, pre-existing)

Not introduced by this merge, but still present at tip after it:

| Surface | State at `af2ab1348` |
|:--|:--|
| `README.md` Homebrew section | Stable `brew install` + tip `--HEAD`; **no** `--with-metal` block (defers to INSTALLATION — OK, but weaker than formula caveats) |
| `README.md` badge / narrative | Still mentions `master` in weekly highlight copy; version badge may lag (see packaging audits) |
| `CMakeLists.txt` `project(… VERSION …)` | Still **2.0.0** (packaging audit residual) |
| Formula caveats | Still “Stable **v2.0.2+** includes the production docking matrix…” while Metal lines correctly say **v2.0.3+** — harmless matrix history note |
| Formula `test do` | Still no Metal symbol / framework proof when `--with-metal` (same residual as #267 F5) |

- Fix recommendation: Optional packaging checklist PR (2.0.4): CMake project version, README badge, README Metal one-liner pointing at INSTALLATION.

### F6. Toolchain / disk guidance is operationally reasonable (INFO)

- Evidence: Metal FATAL_ERROR path in CMake already tells users to run `xcodebuild -downloadComponent MetalToolchain` when metalc/metallib missing — INSTALLATION now mirrors that. “Prefer ≥12 GiB free disk before a full Metal Homebrew rebuild” is operational guidance only (not validated in this audit by a timed rebuild).
- Why it matters: Reduces false “Metal broken” reports from CLT-only SDKs or full disks; aligns with Homebrew from-source cost.
- Fix recommendation: None required.

### F7. Scientific guardrails (INFO)

- No CF / ΔG / free-energy language changes.
- No ranking, clustering, election, DoF budget, or ensemble path edits.
- Metal described as GPU acceleration install path, not as thermodynamic validation.
- Apache-2.0 only; no secrets, no machine-absolute paths in the diff.

### F8. Security / hygiene (INFO)

- Diff is markdown prose and shell command examples already used in formula caveats.
- Tap URL remains `https://github.com/LeBonhommePharma/FlexAIDdS`; qualified formula name `lebonhommepharma/flexaidds/flexaidds` consistent with Homebrew 6+ monorepo-tap pattern documented elsewhere.
- No `.env` or credentials.

---

## Ranking/scoring impact: **NO**

Engine and scoring trees are identical to `342d6650d`. Pose ranking, CF proxy, BindingMode, StatMech, and GA search budgets are untouched.

## Reproducibility impact: **YES (positive, docs)**

| Effect | Detail |
|:--|:--|
| Positive | Users following INSTALLATION for stable Metal no longer chase the obsolete “must use `--HEAD`” path that applied only until v2.0.3. |
| Positive | Version surface in install docs matches tag/formula/Python after #267/#268. |
| Neutral | Does not itself prove a successful Metal brew rebuild on this machine; documents the correct commands. |
| Residual | Experimental Metal status still lives in SUPPORT_MATRIX, not repeated in every INSTALLATION Metal block. |

## Tests adequate: **N/A (docs-only) / PARTIAL for claim surface**

- No code tests required for a pure INSTALLATION edit.
- PR body test plan: Metal shader smoke SUCCESS (5 shaders) and tarball sha match vs formula checked on the branch work; “Docs-only review” was the remaining open item (this audit).
- Formula still does not `nm`/link-test Metal in `test do` (pre-existing; not this PR’s job).
- Self-hosted Metal full gate not exercised by this merge (unchanged workflows).

---

## Cross-links in the 26h swarm

| Commit / PR | Relation |
|-------------|----------|
| `3e059594b` / #260 | Actual Metal bridges on `flexaid_core` (science of the link fix) |
| `1dba43f4b` | Merge of #260 |
| `600e95ba9` / `388e17336` / #267 | v2.0.3 release packaging; remove stable Metal `odie` |
| `00a7b6eae` / `342d6650d` / #268 | Real formula sha256; **first parent of this merge** |
| `80f6ba783` | Sole content commit for INSTALLATION on this PR |
| `e752df7aa` → `4d51e413e` | Transient Metal CI harden + revert on PR branch (net zero) |
| Later #271 / `8c42517bd` | Re-lands self-hosted Metal gate hardening on a proper PR |

---

## Verdict: **DOCS_ONLY / MERGE_OK**

**Approve as landed.** Net change is a correct, overdue alignment of `docs/INSTALLATION.md` with v2.0.3 + stable Homebrew `--with-metal`. Claims are backed by formula and CMake ancestry (PR #260 on tag), not marketing fiction. No ranking or scientific path impact. Optional polish (SUPPORT_MATRIX cross-link; residual CMake/README version drift) is out of scope for this merge and does not justify a block or rewrite of the landed install notes.

**Do not treat this merge as:** Metal GPU docking validation, self-hosted full-gate green, or a complete product version-surface cleanup (CMake `project(VERSION …)` / README badge remain lagging elsewhere).
