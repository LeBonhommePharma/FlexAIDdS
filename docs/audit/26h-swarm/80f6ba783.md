# Audit: 80f6ba783 — Docs: Align INSTALLATION with v2.0.3 stable Metal Homebrew path

**Commit:** `80f6ba78395939a833f11952f317f1c064a25deb`  
**Short:** `80f6ba783`  
**AuthorDate:** 2026-07-15 00:42:38 −0400  
**Author / Committer:** LP `<lp@thebonhomme.com>`  
**Parent:** `342d6650de100537a69907d36a9d3468af132efc` (Merge PR #268 — formula sha256 for v2.0.3)  
**Later land:** merged to `main` via PR **#270** (`af2ab1348`, docs/v2.0.3-install-metal-notes)  
**Audit date:** 2026-07-15  
**Scope:** Docs accuracy audit of this single commit. **Report only — no source edits.**

## Summary (2–4 sentences)

Docs-only patch that brings `docs/INSTALLATION.md` into agreement with the already-shipped **v2.0.3** packaging surface and the **PR #260** Metal `flexaid_core` link fix. It bumps every stale **2.0.2** version note to **2.0.3**, retargets formula `head` language from **`master` → `main`**, and documents stable `brew install --build-from-source --with-metal` (no longer requiring `--HEAD`). All primary claims were checked against the tree at this commit (`Formula/flexaidds.rb`, `CMakeLists.txt`, `VERSION.md`, Python version files, tag ancestry, GitHub PR/release metadata) and hold.

## Severity: LOW

| Dimension | Rating | Notes |
|-----------|--------|-------|
| Overall | **LOW** | Documentation accuracy only; no engine/code changes |
| Docs / operator guidance | **Positive** | Closes a real post-release drift window after v2.0.3 |
| Docking / ranking impact | **None** | Single markdown file |
| False install instructions | **None found** on the claims introduced here |
| Residual surface drift | **LOW** | README badge / CMake `project(VERSION)` still 2.0.0 elsewhere |

## Scope reviewed

| Path | Change | Role |
|------|--------|------|
| `docs/INSTALLATION.md` | **+21 / −8** (29 lines touched across file) | Homebrew install paths, version alignment, Metal GPU section |

```
 docs/INSTALLATION.md | 29 +++++++++++++++++++++--------
 1 file changed, 21 insertions(+), 8 deletions(-)
```

No `LIB/`, `CMakeLists.txt`, `Formula/`, tests, scripts, or ranking paths.

## What the commit changed (claim inventory)

| # | Pre (`80f6ba783^`) | Post (`80f6ba783`) |
|---|--------------------|--------------------|
| 1 | Stable install note: `v2.0.2+` | `v2.0.3+` |
| 2 | No Homebrew Metal install block in macOS easy-install section | Document stable Metal: `brew install --build-from-source --with-metal …` (PR #260) |
| 3 | “Or latest development tip” (no branch name) | “head tracks **main**” |
| 4 | No Metal reinstall example | Commented reinstall: `# brew reinstall --build-from-source --with-metal …` |
| 5 | Versions aligned at **2.0.2** (formula ↔ pyproject ↔ `__version__.py`) | **2.0.3** and also list `VERSION.md` |
| 6 | Homebrew section: no Metal line; “Development / latest” only | Metal GPU stable block + “head tracks main” |
| 7 | Formula “supports stable url/sha256 + head” (no tag/branch) | Explicit `v2.0.3` + `head` on `main` |
| 8 | Maintainer example `TAG=v2.0.2` | `TAG=v2.0.3` |
| 9 | Release notes: `head` tracks **`master`** | `head` tracks **`main`**; Metal install one-liner for v2.0.3+ |
| 10 | Metal GPU section: CMake only; “Requires: macOS with Xcode” | + Homebrew stable path; Xcode + Metal toolchain + `xcodebuild -downloadComponent MetalToolchain` if `xcrun -f metal` fails; prefer ≥12 GiB free disk |

## Evidence matrix (docs claims vs tree / history)

### C1. Version alignment at **2.0.3** — **PASS**

At tree `80f6ba783` (unchanged from parent packaging commits):

| Surface | Value |
|---------|--------|
| `VERSION.md` | **Current Version: 2.0.3** (release notes section for v2.0.3 present) |
| `python/pyproject.toml` | `version = "2.0.3"` |
| `python/flexaidds/__version__.py` | `__version__ = "2.0.3"` / `(2, 0, 3, "final", 0)` |
| `Formula/flexaidds.rb` | `url …/tags/v2.0.3.tar.gz` + non-zero `sha256` |
| Annotated tag | `v2.0.3` → `388e17336` (PR #267 release merge); Metal fix is an **ancestor** |

The parent of this docs commit is already the formula-sha merge (`342d6650d` / #268), so INSTALLATION is not documenting a half-cut release.

### C2. Formula `head` tracks **`main`** — **PASS**

```ruby
# Formula/flexaidds.rb @ 80f6ba783
head "https://github.com/LeBonhommePharma/FlexAIDdS.git", branch: "main"
```

Pre-commit INSTALLATION still said `head` tracks **`master`** (stale after `d9bee5a38` / default-branch work). This commit correctly flips that prose.

### C3. Stable **v2.0.3+** can link Metal without **`--HEAD`** (PR #260) — **PASS**

| Fact | Evidence |
|------|----------|
| Linker fix | `3e059594b` — attach Metal `.mm` bridges + frameworks **PUBLIC** on `flexaid_core` |
| PR merge | `1dba43f4b` — PR **#260** “Homebrew --with-metal link (Metal bridges on flexaid_core)” — **merged** (`state=closed`, `merged_at=2026-07-15T04:31:56Z`) |
| Ancestry into tag | `git merge-base --is-ancestor 3e059594b v2.0.3` → true; same for `1dba43f4b` |
| CMake at tag / this commit | Block “Metal sources (attach to flexaid_core once)”: `target_sources(flexaid_core PRIVATE …metal_eval.mm…)` + `target_link_libraries(flexaid_core PUBLIC ${METAL_LIBRARY} …)` |
| Formula gate removed | Pre-v2.0.3 formula (`5a3b95430` era) had `odie` when `build.with?("metal") && !build.head?`; release `600e95ba9` / tag tree **removes** that gate |
| Formula caveats @ this commit | Same command string as INSTALLATION: `brew install --build-from-source --with-metal lebonhommepharma/flexaidds/flexaidds` |

Doc wording “no longer requires `--HEAD` for a clean Metal link” is scoped to **stable v2.0.3+** — accurate; v2.0.2 stable still needed HEAD/odie.

### C4. Homebrew command form matches formula — **PASS**

Documented install:

```bash
brew install --build-from-source --with-metal lebonhommepharma/flexaidds/flexaidds
```

Matches:

- Formula `option "with-metal", …`
- Formula caveats install + reinstall lines
- Need for `--build-from-source` / `-s` when options must apply (bottles ignore options; doc also states bottles may come later)

Qualified tap name `lebonhommepharma/flexaidds/flexaidds` matches the monorepo-as-tap pattern already used throughout INSTALLATION and formula caveats.

### C5. Metal toolchain guidance — **PASS (with soft heuristic)**

CMake at this commit fatals when Metal is ON without tools:

```text
FLEXAIDS_USE_METAL=ON but no executable Metal compiler was found.
Run: xcodebuild -downloadComponent MetalToolchain
```

INSTALLATION’s post-change “Requires: macOS with Xcode + Metal toolchain (`xcodebuild -downloadComponent MetalToolchain` if `xcrun -f metal` fails)” **matches** the build system’s own error path. The “≥12 GiB free disk” preference is **advisory** (not enforced in formula/CMake); not false, not evidence-backed in-repo.

### C6. Maintainer `TAG=v2.0.3` example — **PASS**

Consistent with live annotated tag and formula URL. Sha refresh recipe (`curl … | shasum -a 256`) matches how `00a7b6eae` produced the formula digest.

### C7. Scientific language — **PASS / N/A**

No claims about ΔG, CF scoring as free energy, ranking, GA budget, or ensemble thermodynamics. Patch stays in packaging/install space (AGENTS.md CF-proxy vs thermo boundary not violated).

## Findings

### F1. Commit does its job: closes post-v2.0.3 INSTALLATION drift (INFO / positive)

- **Evidence:** Parent tree already had formula/Python/`VERSION.md` at 2.0.3 and stable Metal caveats; INSTALLATION still advertised 2.0.2 and `head`→`master`, and omitted the stable Metal brew path. This commit is the expected follow-up called out in the v2.0.3 release audit (`388e17336` F4: INSTALLATION lag fixed by `80f6ba783` / #270).
- **Why it matters:** Operators reading INSTALLATION (not formula caveats) would otherwise install “current” docs that lag the tag by a critical Metal workflow change.
- **Fix recommendation:** None for this commit. Keep a release checklist item: “INSTALLATION Homebrew version + Metal path.”

### F2. Primary factual claims verified true (INFO / positive)

- See evidence matrix C1–C6. No contradictory command or version string was introduced relative to `Formula/flexaidds.rb` and the tagged CMake Metal membership model.
- GitHub API (session): PR #260 merged; release tag `v2.0.3` present (non-draft, non-prerelease). Full tarball re-hash was **not** completed in-session (curl truncated under timeout); digest correctness is attributed to dedicated commit `00a7b6eae` already on the parent chain, not re-proven here.

### F3. Metal reinstall example is commented in one place only (LOW — UX consistency)

- **Evidence:** macOS “Easy install” block ends with:

  ```bash
  # Metal reinstall after tap update:
  # brew reinstall --build-from-source --with-metal lebonhommepharma/flexaidds/flexaidds
  ```

  Formula caveats show the same reinstall line **active** (not commented). Other INSTALLATION sections document only `install`, not reinstall.
- **Why it matters:** Users upgrading an existing CPU-only Cellar build may miss the reinstall path if they only skim the first code fence.
- **Fix recommendation (future docs, not this audit’s scope):** Uncomment reinstall in the primary macOS fence, or one prose sentence: “Already installed without Metal? use `brew reinstall --build-from-source --with-metal …` after `brew update`.”

### F4. Triple duplication of the Metal brew command (LOW — maintenance risk)

- **Evidence:** Identical stable Metal install line appears in (1) macOS easy-install, (2) Homebrew section, (3) Build Variants → Metal GPU, plus a fourth mention in Releasing & Distribution prose.
- **Why it matters:** Future version bumps (2.0.4+) can partially update and re-create the exact drift this commit fixes.
- **Fix recommendation:** Prefer one canonical Homebrew Metal subsection and cross-links; optional `scripts/check_repo_hygiene.py` / release script grep for version literals in INSTALLATION.

### F5. Related surfaces still stale at this commit (LOW — out of file scope, residual product consistency)

Not introduced by this commit; still true of the **tree** when it lands:

| Surface @ `80f6ba783` | State |
|-----------------------|--------|
| `README.md` version badge | still `version-2.0.0` |
| `README.md` Homebrew block | tap + install + HEAD only; **no** `--with-metal` (points to INSTALLATION for full detail — acceptable) |
| `CMakeLists.txt` `project(… VERSION …)` | remains **2.0.0** (historical packaging gap; not docs) |
| Formula caveats matrix line | still “Stable **v2.0.2+** includes the production docking matrix” — historically true, not contradicted by this commit |

- **Why it matters:** Multi-file version truth is still not single-sourced; INSTALLATION is now correct while README badge is not.
- **Fix recommendation:** Separate packaging hygiene PR (badge + CMake VERSION + optional README Metal one-liner). Do not fold into rewriting this commit’s accuracy.

### F6. “v2.0.3+” requires a fresh tap formula (LOW — operator footgun, wording OK)

- **Evidence:** Metal-on-stable works only when the **live** Homebrew formula points at the v2.0.3 (or later) tarball **and** no longer runs the old `odie` gate. A machine that last `brew update`d before #267/#268/#270 can still hold a v2.0.2 formula that refuses or fails Metal.
- **Why it matters:** Doc says “stable v2.0.3+” which is correct for **source capability**, not “whatever is already installed in Cellar.”
- **Fix recommendation (optional future prose):** One line: “Requires `brew update` so the tap formula is ≥2.0.3.”

### F7. No engine, ranking, or licensing issues (INFO)

- Diff is pure Markdown under `docs/`.
- No secrets, absolute user paths, or GPL text.
- Apache-2.0 project licensing unaffected.
- CF / ranking / GA budget / StatMech paths byte-identical to parent.

### F8. Process placement is correct (INFO / positive)

- Landed **after** formula URL + real sha256 (`00a7b6eae` / #268 → parent merge `342d6650d`).
- Landed **after** the scientific/link fix and the packaging release that removed the stable Metal refuse gate.
- Merged publicly as PR **#270** (`af2ab1348`). Subject/body match the diff (no overclaim of code fixes).

## Ranking/scoring impact: NO

Documentation only. Pose ranking, CF/contact-function scoring proxy, clustering order, GA DoF budget (pop vs gen), and thermodynamic ledger code paths are untouched.

## Reproducibility impact: YES (docs/operator path only)

**Positive:** End users following INSTALLATION can now install **stable** Metal the same way formula caveats and v2.0.3 release notes intend, without being steered to `--HEAD` or stale 2.0.2 tags.

**Negative:** None introduced by false instructions. Residual: README badge still 2.0.0; multi-section duplication risk; ≥12 GiB disk note is soft.

## Tests adequate: N/A (docs-only) / process OK

- No code tests required for this commit class.
- Accuracy verification performed via `git show`, tree greps, ancestry checks, formula/CMake cross-read, and GitHub PR/release metadata.
- Did **not** run a live `brew install --with-metal` in this audit (disk/time; would re-validate packaging already claimed by #260 + #267, not this markdown).

## Related history (context)

| SHA / PR | Role relative to this commit |
|----------|------------------------------|
| `3e059594b` / **#260** | Real Metal `flexaid_core` OBJCXX membership fix |
| `1dba43f4b` | Merge of #260 |
| `600e95ba9` / **#267** | v2.0.3 packaging; remove stable Metal `odie` |
| `388e17336` | Annotated tag `v2.0.3` |
| `00a7b6eae` / **#268** | Formula sha256 for tarball |
| `342d6650d` | Parent of this commit (merge #268) |
| **`80f6ba783` / #270** | **This commit** — INSTALLATION alignment |
| `af2ab1348` | Merge PR #270 onto main |

## Verdict: APPROVE

**Docs accuracy: clean.** The commit subject and body match the diff; every version, branch, PR #260, and stable Metal Homebrew claim introduced here is consistent with the formula, CMake Metal attachment model, tag ancestry, and release history at this SHA. No source edits required. Residual product-version drift lives in **other files** (README badge, CMake `project` VERSION) and should be tracked as packaging hygiene, not as a defect of this documentation fix.
