# Audit: `97da54e75` — LIVE_QUEUE realism vs unfinished FO dual-suffix / SoftBeta

| Field | Value |
|-------|--------|
| **Short** | `97da54e75` |
| **Full** | `97da54e75b4c42da8f803bcf12327523cdacde5a` |
| **Subject** | Docs: Prioritize three-engine Astex 85 as primary live science goal |
| **Author / date** | LP · 2026-07-15 01:04:32 -0400 |
| **Audit date (UTC)** | 2026-07-15 |
| **Auditor** | Grok Build (26h commit-audit swarm) |
| **Scope** | Docs-only commit; **no source edits** in this audit. Focus: `LIVE_QUEUE.md` operational/scientific realism against unfinished **FO dual-suffix packaging** and **SoftBeta ranking identity**, plus protocol drift and live machine state. |
| **Parents** | docs-only tree on `docs/three-engine-priority-goal` (merged lineage via later PRs; on `main` as of audit) |

---

## 1. Executive verdict

| Dimension | Verdict | One-line |
|-----------|---------|----------|
| **Commit intent** | **Sound** | Correctly elevates matched three-engine Astex 85 over oracle-ceiling restore. |
| **LIVE_QUEUE as agent contract** | **Partially realistic / stale** | Priority framing OK; hard invariants and queue status lag claim freeze + local-first policy. |
| **vs unfinished FO dual-suffix** | **Material gap** | Shared `enumerate_emitted_cluster_heads()` **not on `main`**; BCR/consensus/Hvib still single-suffix-only. FO arm B packaging incomplete for claim-grade S3/BCR and full election pooling. |
| **vs unfinished SoftBeta** | **Material gap** | `LIB/SoftBetaFreeEnergy.h` **absent on `main`**; docs and `fix/softbeta-ranking-identity` exist, engine/DatasetRunner not single-sourced. |
| **Claim readiness of C0 path** | **Overstated in LIVE_QUEUE** | C0 is not pure “CF election”; live claim clean uses `[3DSIG-RANK]` + CF clustering. Not the 2017 FO entropy red bar until dual-suffix + FO clustering identity validated. |
| **Overall risk** | **HIGH for agents that treat LIVE_QUEUE as executable truth** | Can launch wrong budget (gen 6000), wrong B TEMPER (298 vs 21), iCloud-live I/O, and over-interpret C0 S1 as 3Dsig FlexAIDdS. |

**Bottom line:** `97da54e75` is a good **priority signal**, not a verified **ops freeze**. Agents must **not** treat `LIVE_QUEUE.md` hard invariants as superseding `benchmarks/protocols/three_engine_entropy_comparison.md`, `AGENTS.md` storage rules, or the open FO dual-suffix / SoftBeta branches.

---

## 2. What the commit actually changed

Diffstat (pure docs):

```
.agents/skills/flexaidds-benchmarking/LIVE_QUEUE.md | 87 ++++++++++++++++++++++
.agents/skills/flexaidds-benchmarking/SKILL.md     |  2 +
2 files changed, 89 insertions(+)
```

### 2.1 `LIVE_QUEUE.md` (new)

Introduces a machine-facing “primary live goal”:

- Arms **A / B0 / B / C0 / C** on Astex Diverse **N=85**, no native seed, single matrix pin.
- Deprioritizes oracle-ceiling restore (seeded diagnostic).
- Hard invariants table: matrix MD5, search budget, seed ban, **iCloud only**, RAM serialization.
- Queue status snapshot: C0 full85 **LIVE**, A→B0→B pilot **PAUSED**, full N=85 blocked.
- Resume/aggregate shell snippets.

### 2.2 `SKILL.md` (+2 lines)

Points all benchmark agents at `LIVE_QUEUE.md` as the primary live goal.

No C++, CMake, scripts, or tests in this commit.

---

## 3. Audit focus A — FO dual-suffix (unfinished on `main`)

### 3.1 Engine emission contract (on `main`)

FO heads are **dual-suffix** from BindingMode:

```text
// LIB/BindingMode.cpp (main)
snprintf(sufix, sizeof(sufix), "_%d_%d.pdb", minPoints, num_result);
// → <prefix>_<minPts>_<rank>.pdb
```

CF / DensityPeak remain single-suffix: `<prefix>_<rank>.pdb`.

Normative docs:

- `docs/implementation/3dsig_shannon_ranking.md` §3 — DatasetRunner **must enumerate both**.
- `docs/implementation/3dsig_red_pair_protocol.md` §2 — “C0 FlexAIDdS DatasetRunner packaging is **out of band** until FO dual-suffix election is verified.”

### 3.2 What `main` has today (partial)

| Path | Dual-suffix aware? | Evidence (`main` `DatasetRunner.cpp`) |
|------|--------------------|----------------------------------------|
| S1 election candidate pool | **Partial yes** | ~L1072: directory scan for `_digits_digits.pdb` after single-suffix loop |
| BCR / best_cluster_rmsd scan | **No** | ~L6927: only `pfx + "_" + pi + ".pdb"` |
| BCR-gate path rebuild | **No** | Rebuilds from index as single-suffix; FO cannot rebuild from idx alone |
| Consensus / Hvib / cached-prefix discovery | **No** | ~L779, L840, L6639, L6677 — single-suffix only |
| Shared helper + unit tests | **No** | `enumerate_emitted_cluster_heads` **absent** on `main` |

### 3.3 Finished work that is **not merged**

| Commit | Branch | Status vs `main` |
|--------|--------|------------------|
| `db9302d76` Fix: FO dual-suffix enumeration for DatasetRunner election and BCR | `fix/fo-dual-suffix-packaging` | **Not ancestor of `main`** |
| `095860e51` Fix: complete FO dual-suffix packaging for election and BCR | same | **Not ancestor of `main`** |

`095860e51` introduces the intended completion:

- Shared `enumerate_emitted_cluster_heads()` / `EmittedClusterHead` in `DatasetRunner.h`
- Wire election, BCR scan, BCR-gate (full path), consensus, Hvib, cached-prefix discovery
- Unit tests in `tests/test_dataset_runner.cpp` (`EmittedClusterHeads::*`)

Until that lands and is binary-pinned, **any FO emission (arm B, FO pilot arms, future C with FO)** can:

1. Elect from dual-suffix heads in one code path while BCR still sees **empty / wrong** single-suffix names → S1/S3 inconsistency.
2. Fail BCR-gate override (path rebuilt as `prefix_i.pdb` when head is `prefix_minPts_i.pdb`).
3. Under-report packaging success for FO campaigns.

### 3.4 Interaction with LIVE_QUEUE

LIVE_QUEUE:

- Labels arm **B** as `TEMPER 298`, BindingMode / **FO path**.
- Labels **C0** as “CF / standard DatasetRunner election (**claim path**)” and marks C0 full85 **LIVE**.
- Does **not** mention dual-suffix validation as a gate.

**Realism:**

- Treating **C0** as the primary live claim path while FO dual-suffix packaging is unfinished is only defensible if C0 stays on **CLUSTA CF** (single-suffix). Live claim-clean logs show `Cluster: CF` — so C0 S1 packaging is **less** blocked by dual-suffix than arm **B**.
- Treating **B** (or 3Dsig red-pair FlexAIDdS bar) as claim-ready **is not** defensible until `fix/fo-dual-suffix-packaging` is merged, tested (`ctest`), and the claim binary is rebuilt/pinned.
- LIVE_QUEUE’s silence on this gate **contradicts** `3dsig_red_pair_protocol.md` / `3dsig_shannon_ranking.md`, which still freeze C0 packaging as **out of band** for the 2017 red-bar story.

**Severity for LIVE_QUEUE readers:** **HIGH** for B/FO science; **MEDIUM** for C0 CF packaging (election dual-suffix scan is partial; BCR still wrong if FO ever enabled).

---

## 4. Audit focus B — SoftBeta ranking identity (unfinished on `main`)

### 4.1 Documented contract

`docs/implementation/3dsig_shannon_ranking.md` states:

- Soft-β \(\tilde G = \tilde H - T\tilde S \equiv E_{\min}-T\ln Z\) (ACF).
- Shared implementation: **`LIB/SoftBetaFreeEnergy.h`**.
- Layers that **must stay identical**: `cluster.cpp` ACF, `BindingMode::compute_energy`, DatasetRunner S1 (`[3DSIG-RANK]`).

### 4.2 Reality on `main`

| Artifact | On `main`? |
|----------|------------|
| `LIB/SoftBetaFreeEnergy.h` | **No** (file missing) |
| DatasetRunner inline \(\tilde G\) election (`c82e6fc24`) | **Yes** — default `election_shannon_free_energy` |
| BindingMode classic soft-β (local formulas, not shared header) | **Yes** — separate path |
| `cluster.cpp` ACF | Local ACF math (not SoftBeta header) |
| Shared SoftBeta unit tests (`SoftBetaIdentity::*`) | Only on `fix/softbeta-ranking-identity` |

Unmerged branch:

| Commit | Branch | Content |
|--------|--------|---------|
| `2129c1bf4` Add: SoftBeta ranking identity (\(\tilde G=\tilde H-T\tilde S\) ≡ ACF) | `fix/softbeta-ranking-identity` | Adds `SoftBetaFreeEnergy.h`; wires cluster / BindingMode / DatasetRunner; classic ranking tests |

**Not ancestor of `main`.**

### 4.3 Why this matters for LIVE_QUEUE

LIVE_QUEUE arm table:

| LIVE_QUEUE claim | Code truth (main / live receipt) |
|------------------|-----------------------------------|
| **C0** = “CF / standard DatasetRunner election (claim path)” | **Stale.** Default election is **Shannon \(\tilde G\)** (`[3DSIG-RANK]`), not pure min-CF. Live `RUN_RECEIPT.json` for C0 claim clean has `"election_shannon_free_energy": true`, `"election_soft_T": 298`. |
| **B** entropy on at TEMPER **298** | Protocol claim freeze + `run_B_pilot8.sh`: **TEMPER 21**. |
| Entropy isolation C re-rank “later” | SoftBeta unmerged → re-rank identity across engine emission vs DatasetRunner still **not** single-sourced. |

Without SoftBeta merge:

- Risk of **silent objective drift** between emission order (ACF / BindingMode) and DatasetRunner election (inline copy of \(\tilde G\)).
- Docs advertise a header that **does not exist** → agent/tooling confusion.
- LIVE_QUEUE’s C0 label (“CF election”) will cause agents to mis-report the ranking product of any C0 full85 campaign that already logs `[3DSIG-RANK]`.

**Severity:** **HIGH** for scientific language and entropy-isolation claims; **MEDIUM** for pure A/B0 CF engine-generation contrasts.

---

## 5. Hard-invariant realism (LIVE_QUEUE table vs normative freeze)

| LIVE_QUEUE invariant | Normative / live reality | Realistic? |
|----------------------|--------------------------|------------|
| Dataset Astex Diverse **85** | Matches three-engine protocol + 3Dsig | **Yes** |
| Matrix `MC_st0r5.2_6.dat` MD5 **`72d7c739…`** | Queue `data/` pin = `72d7c739…`; many **repo** copies = `9dc93717…` | **Yes if** jobs use queue/local pin only; **dangerous** if agents hash `./MC_st0r5.2_6.dat` in repo |
| Search pop **1000** · gen **6000** · restarts **5** · T **298** | Claim freeze 2026-07-15: gen **2000** fixed; DoF scales **pop** (`EVAL_SCALE_DIHEDRAL=1`). Scripts: `run_C0_claim_clean.sh` GEN=2000; FlexAID pilot GEN default 2000. B **TEMPER 21**. | **No — stale** |
| Seed forbidden for claim | Matches admission contract / three-engine | **Yes** |
| Storage **iCloud only** | `AGENTS.md` + `docs/ICLOUD_BENCHMARK_STORAGE.md`: **local-first / thin-iCloud**. Live GA OUT on `$FLEXAIDDS_LOCAL_ROOT`. iCloud is durable mirror only. | **No — actively wrong** |
| RAM ~18 GiB, one heavy campaign | Matches red-pair + pilot practice | **Yes** |

### 5.1 Budget smoking gun

Older queue log `C0_full85.log` (pre claim-clean):

```text
[EVAL-SCALE] 1G9V: FIXED pop=1000 n_gen=6000 (no DoF scaling)
```

That is the **anti-pattern** called out in `three_engine_entropy_comparison.md` (mode -1 / fixed 6000). LIVE_QUEUE hard-coding gen **6000** trains agents to re-create it.

Claim-clean receipt (local):

```json
"gen": 2000,
"matrix_md5": "72d7c7396702331d96ff12d18f831796",
"election_shannon_free_energy": true,
"eval_scale_dihedral": 1
```

So even on the same day as `97da54e75`, the **actual** preferred C0 claim path already diverged from LIVE_QUEUE’s search row.

---

## 6. Queue-status realism (point-in-time fiction)

LIVE_QUEUE (written ~05:04Z):

| Job | LIVE_QUEUE status | Audit-time machine check |
|-----|-------------------|---------------------------|
| Oracle-ceiling | DONE | Accepted as historical; not re-validated here |
| **C0 full85** | **LIVE** via `$FLEXAIDDS_QUEUE_ROOT/logs/C0_full85.pid` | **`C0_full85.pid` absent.** Log exists but stale (~03:15). Claim-clean campaign dirs exist local + iCloud; **no live FlexAIDdS/benchmark_datasets C0 process** |
| A→B0→B pilot8 | **PAUSED** (SIGSTOP) | **False.** Live process: `bin/A/FlexAID --legacy` on **1GPK restart_5** under `campaigns/three_engine/A/3dsig_r10/` (arm A running, not paused) |
| Full A/B0/B N=85 | Blocked | Plausible, but secondary to active 3Dsig A launch |
| Entropy isolation C re-rank | Later | Still blocked by SoftBeta + frozen ensembles |

**Priority conflict with later science docs:**

| Source | Priority |
|--------|----------|
| LIVE_QUEUE (`97da54e75`) | Advance **C0** first; A/B after C0 |
| `3dsig_red_pair_protocol.md` | **A→B0→B** priority; C0 **STOPPED / SUSPENDED** until FO dual-suffix verified |

Audit-time process table matches the **red-pair** priority, not LIVE_QUEUE’s C0-first snapshot.

---

## 7. Scientific framing scorecard

| LIVE_QUEUE statement | Assessment |
|----------------------|------------|
| Primary question: matched no-seed, single matrix, A vs B0 vs B vs C0/C | **Correct** scientific framing |
| Oracle-ceiling is not the claim path | **Correct** |
| S1/S2 primary; S3/BCR diagnostic | **Correct** vs admission contract |
| C0 = CF election claim path | **Incorrect / stale** after `c82e6fc24` (Shannon \(\tilde G\) default) |
| B = TEMPER 298 FO | **Incorrect** vs TEMPER **21** claim freeze; FO packaging unfinished |
| C = full stack, re-rank later | Directionally right; SoftBeta unfinished weakens “same objective” re-rank story |
| Deprioritize packaging/fleet until three-engine lands | Reasonable **if** dual-suffix/SoftBeta are not required gates — but they **are** gates for FO/B and 3Dsig identity |

---

## 8. Dependency graph (what must finish for LIVE_QUEUE goals to be real)

```text
                    ┌─────────────────────────────┐
                    │ LIVE_QUEUE three-engine 85  │
                    └──────────────┬──────────────┘
           ┌───────────────────────┼───────────────────────┐
           ▼                       ▼                       ▼
   A / B0 (CF only)         B (FO + TEMPER 21)      C0 / C (DatasetRunner)
   packaging OK-ish         needs FO dual-suffix    needs truthful election
   matrix pin + gen2000     packaging MERGED        label (not "CF only")
           │                       │                       │
           │                       ▼                       ▼
           │              fix/fo-dual-suffix-         SoftBeta identity
           │              packaging → main            (optional for C0 CF
           │              + ctest + pin binary         elect; required for
           │                                          ranking identity /
           │                                          C re-rank / 3Dsig)
           ▼                       ▼                       ▼
        pilot8 gate ──► full85 A/B0/B          claim S1/S2 tables
```

**Minimum gates before advertising “claim path complete” for FO/entropy arms:**

1. Merge `fix/fo-dual-suffix-packaging` → `main`; `ctest` green; rebuild & pin C (and B if applicable).
2. Merge `fix/softbeta-ranking-identity` **or** explicitly document that inline DatasetRunner \(\tilde G\) is the temporary single source and BindingMode/cluster still match by review.
3. Rewrite LIVE_QUEUE hard invariants: gen **2000**, B **TEMPER 21**, storage **local-first**, C0 election **Shannon \(\tilde G\)** (unless `ELECTION_LEGACY_ZH=1`).
4. Refresh queue status from `ps` / local pid files — never trust a frozen LIVE table.

---

## 9. Positive findings

1. **Priority signal is right:** three-engine no-seed Astex 85 over seeded oracle-ceiling is the correct science ordering.
2. **Admission language** (S1/S2 vs BCR; seed_echo=0) aligns with `admission_metrics_contract.md`.
3. **SKILL.md pointer** reduces multi-agent goal thrash.
4. **RAM serialization** rule is operationally necessary on this Mac.
5. **Matrix MD5 string** in LIVE_QUEUE matches the **queue pin** used by claim-clean receipt (`72d7c739…`) — good, provided agents do not hash stray repo copies (`9dc93717…`).

---

## 10. Findings summary (severity-ranked)

| ID | Severity | Finding |
|----|----------|---------|
| F1 | **HIGH** | FO dual-suffix packaging **unfinished on `main`** (BCR/consensus/Hvib single-suffix). Branch `fix/fo-dual-suffix-packaging` complete but unmerged. LIVE_QUEUE never gates on this. |
| F2 | **HIGH** | SoftBeta shared header **missing on `main`**; docs claim `LIB/SoftBetaFreeEnergy.h`. Ranking identity unfinished. |
| F3 | **HIGH** | LIVE_QUEUE search budget **gen 6000** + thermo T **298 for B** contradicts claim freeze (**gen 2000**, B **TEMPER 21**). |
| F4 | **HIGH** | Storage **“iCloud only”** contradicts `AGENTS.md` local-first / thin-iCloud; risk of FileProvider hangs on live GA. |
| F5 | **HIGH** | C0 arm labeled **“CF election”** but main + live claim-clean use **`[3DSIG-RANK]`** Shannon \(\tilde G\). |
| F6 | **MEDIUM** | Queue status **stale**: C0 not LIVE; arm **A 3dsig_r10 is LIVE**; pilot not SIGSTOP-paused as claimed. |
| F7 | **MEDIUM** | Priority conflict with `3dsig_red_pair_protocol.md` (A→B0→B first; C0 out of band until dual-suffix verified). |
| F8 | **LOW** | Dual MD5 hazard for matrix filename across repo vs queue data — protocol already warns; LIVE_QUEUE only shows one MD5. |
| F9 | **INFO** | Commit itself is docs-only and **safe** as a priority memo; risk is **downstream agent execution**, not the patch content. |

---

## 11. Recommended LIVE_QUEUE corrections (do **not** implement in this audit)

Docs-only suggestions for a follow-up commit (not performed here):

1. **Hard invariants**
   - Search: pop base 1000 · gen **2000** fixed · restarts 5 · `EVAL_SCALE_DIHEDRAL=1`.
   - Arm B: **TEMPER 21** (not 298).
   - Storage: **local-first** live OUT; iCloud thin mirror only.
2. **Arm table**
   - C0: “DatasetRunner election = soft-β \(\tilde G\) (default ON); clustering **CF** for current claim-clean.”
   - B: FO + TEMPER 21; **blocked on FO dual-suffix merge for full BCR/election packaging**.
3. **Open gates section**
   - [ ] Merge FO dual-suffix packaging + tests  
   - [ ] Merge SoftBeta identity **or** freeze inline \(\tilde G\) as temporary SoT  
   - [ ] Pin claim binaries after merges  
4. **Queue status**: regenerate from live pids; never ship multi-hour-stale LIVE/PAUSED without a timestamp + “verify with `ps`” note.
5. **Deprioritized list**: keep oracle-ceiling deprioritized; do **not** deprioritize FO dual-suffix / SoftBeta — they are **science unblocks**, not hygiene.

---

## 12. Evidence index (absolute paths / refs)

| Item | Location |
|------|----------|
| Audited commit | `97da54e75b4c42da8f803bcf12327523cdacde5a` |
| LIVE_QUEUE | `/Users/lp.more/Projects/FlexAIDdS/.agents/skills/flexaidds-benchmarking/LIVE_QUEUE.md` |
| Three-engine protocol | `/Users/lp.more/Projects/FlexAIDdS/benchmarks/protocols/three_engine_entropy_comparison.md` |
| 3Dsig ranking contract | `/Users/lp.more/Projects/FlexAIDdS/docs/implementation/3dsig_shannon_ranking.md` |
| 3Dsig red-pair protocol | `/Users/lp.more/Projects/FlexAIDdS/docs/implementation/3dsig_red_pair_protocol.md` |
| Storage policy | `/Users/lp.more/Projects/FlexAIDdS/AGENTS.md` § Benchmark storage |
| FO emission | `LIB/BindingMode.cpp` dual-suffix `_%d_%d.pdb` |
| Partial election dual-suffix | `LIB/DatasetRunner.cpp` ~L1072 (main) |
| Incomplete BCR | `LIB/DatasetRunner.cpp` ~L6927 single-suffix (main) |
| FO packaging branch | `fix/fo-dual-suffix-packaging` @ `095860e51` / `db9302d76` |
| SoftBeta branch | `fix/softbeta-ranking-identity` @ `2129c1bf4` |
| Shannon election on main | `c82e6fc24` |
| Live C0 claim receipt | `~/flexaidds_results/campaigns/C0_full85_claim_g2000_popmod_20260715/RUN_RECEIPT.json` |
| Live A process | `…/bin/A/FlexAID --legacy` → `…/A/3dsig_r10/1GPK/1GPK_r5` |
| Queue matrix MD5 | `72d7c7396702331d96ff12d18f831796` |
| Repo tree matrix MD5 (example) | `9dc93717dfed0698006d88dd6a9627bc` |

---

## 13. Conclusion

`97da54e75` successfully **reorients agent attention** toward the right science goal (three-engine Astex 85, no seed, oracle-ceiling demoted). As an executable ops contract, **`LIVE_QUEUE.md` is not yet realistic**:

- It **understates** unfinished FO dual-suffix packaging and SoftBeta identity work that gate FO/B and ranking reproducibility.
- It **overstates** C0 readiness and mislabels C0 election as pure CF.
- Several **hard invariants are stale** relative to the same-day claim freeze (gen, B TEMPER, storage).
- Queue status was a **snapshot** that no longer matches the machine (A live; C0 not LIVE).

**Do not claim FO dual-suffix or SoftBeta “done” from this commit — they are not in the diff and not on `main`.**  
**Do not run claim campaigns from LIVE_QUEUE’s gen=6000 / iCloud-only / B@298 rows without re-checking protocols and live pids.**
