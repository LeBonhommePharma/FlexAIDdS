# Audit: 711b83cc9 — Merge branch main into feature/bonhomme-fleet-dataset-runner-v1

## Summary (2–4 sentences)
Merge commit `711b83cc9ab24f81ff92cb23f9d659a3fb9172b6` (GitHub merge, 2026-07-14 20:38:15 -0400) brings `main` tip `4beb3b36d` into feature tip `2fc7189d8`. The **only byte-level delta vs first parent** is a trailing newline on a **17-line non-functional CMakeLists.txt stub** — no C++/Python scoring, GA, or DatasetRunner logic changes. Both parents had already destroyed the real ~3k-line root `CMakeLists.txt` (main: 1-line Grok placeholder; feature: 16-line Fleet stub that does not call `project()` / `cmake_minimum_required`). The merge **prefers the feature stub (ours)** and, 15 seconds later, is the tip landed by PR #257 (`8eaf043ae`), planting a build-dead root CMake on `main` until `292a0ce6a` restored the real file from `master`. Pre-existing fleet “integration” commits on this tip also left `LIB/DatasetRunner.h` and `LIB/FleetRunner.cpp` as comment placeholders — outside this merge’s diff, but inseparable from the branch tip the merge freezes.

## Severity: CRITICAL

## Parents / topology

| Role | SHA | Subject |
|------|-----|---------|
| Merge | `711b83cc9` | Merge branch `main` into `feature/bonhomme-fleet-dataset-runner-v1` |
| First parent (ours / feature) | `2fc7189d8` | fix(build): Update CMakeLists.txt to include FleetRunner… |
| Second parent (theirs / main) | `4beb3b36d` | CMake: entropy collapse — modular targets… |
| Merge-base | `d8a422f93` | Chore: ignore `.tools/` and `.venv*/` |

- Commits on main not in feature at merge time: **1** (`4beb3b36d` only).
- Commits on feature not in main: **189** (large science/fleet stack).
- Overlapping paths changed on both sides since merge-base: **`CMakeLists.txt` only**.
- Delta first parent → merge: `CMakeLists.txt` **+1 newline only** (`git diff 2fc7189d8 711b83cc9` = trailing newline fix).
- Delta second parent → merge: entire feature tree (thousands of files) — expected for merge-into-feature.
- Downstream land: `8eaf043ae` = Merge PR #257 (`4beb3b36d` + `711b83cc9`) 15s later.

## Findings

### F1. Merge solidifies a build-dead root CMakeLists (CRITICAL)
- Evidence: Three-way sizes (blob content):
  - Merge-base `d8a422f93`: **3226** lines — full FlexAID `project()`, OBJCXX, sources, tests.
  - Main `4beb3b36d`: **1** line — `# New modular top-level CMakeLists.txt ... (full content from Grok's previous)` (no newline).
  - Feature `2fc7189d8`: **16** lines — Fleet stub only (no trailing newline).
  - Merge `711b83cc9`: **17** lines — feature stub + trailing newline.
- Merge resolution: **ours** (feature stub wins). Diff vs first parent is solely `\ No newline at end of file` → newline. No attempt to restore merge-base or `2fc7189d8^` (2948-line real CMake that existed 3 minutes earlier).
- Stub contents at merge (entire functional surface):
  ```cmake
  # Updated CMakeLists.txt with Bonhomme Fleet support
  add_library(FleetRunner LIB/FleetRunner.cpp)
  target_link_libraries(FleetRunner PUBLIC CoreLib Metal::Metal)
  target_sources(flexaidds PRIVATE tools/metal_microbench.cpp)
  if(BUILD_TESTING)
      add_executable(test_fleet_runner tests/cpp/test_fleet_runner.cpp)
      target_link_libraries(test_fleet_runner PRIVATE FleetRunner gtest)
  endif()
  ```
- Why it matters: `cmake -S . -B build` cannot define a project. Missing: `cmake_minimum_required`, `project()`, `CMAKE_CXX_STANDARD`, `add_subdirectory`/`include` of real modules, FlexAID executable, tests matrix, provenance defines. Violates AGENTS.md “fresh builds after CMake/source changes” and “verify with actual execution.”
- Fix recommendation (historical): Conflict resolution should have taken **master/pre-destroy** CMake (or `2fc7189d8^` / merge-base) and **added** Fleet targets into it — never replace the monolith with a sketch. Done later by `292a0ce6a` (“Resolve CMakeLists.txt conflict by taking master (2948-line real build file) over main's 17-line fleet stub from PR #257”).

### F2. Stub references undefined targets and missing sources (CRITICAL)
- Evidence at tree `711b83cc9`:
  | Reference | Status |
  |-----------|--------|
  | `CoreLib` | **No** `add_library(CoreLib…)` anywhere in tree |
  | `Metal::Metal` | No `find_package`/imported target setup in root CMake |
  | `flexaidds` (target_sources) | No prior `add_executable`/`add_library(flexaidds…)` |
  | `tools/metal_microbench.cpp` | **MISSING** (exists: `tools/metal_microbench_enhanced.cpp` placeholder) |
  | `tests/cpp/test_fleet_runner.cpp` | **MISSING** (exists: `tests/test_fleet_benchmark.py` placeholder) |
  | `LIB/FleetRunner.cpp` | EXISTS but is a 2-line comment stub (F3) |
  | `gtest` | Not fetched/linked from root |
- Modular helpers under `cmake/FlexAID*.cmake` exist in the tree but are **not included** by the stub.
- Why it matters: Even if someone hand-patched `project()`, configure/link would still fail. The “fix(build): … Clean integration” claim on parent `2fc7189d8` is false on inspection.
- Fix recommendation: Wire Fleet into real `CMakeLists.txt` with correct paths (`metal_microbench_enhanced.cpp` or rename), real library names (`flexaid_core` / existing targets), and a real gtest target under `BUILD_TESTING`.

### F3. Branch tip fleet “integration” is placeholder theater (CRITICAL — pre-merge, frozen by merge)
- Evidence (present at merge tip; introduced by `69aa0fab6` / `2fc7189d8`, not rewritten by merge):
  - `LIB/FleetRunner.cpp` entire content:
    ```cpp
    // Full implementation of FleetRunner as detailed earlier
    #include ... // complete code snippet for sharding, dispatch, aggregation
    ```
  - `LIB/DatasetRunner.h`: **1 line** at merge (`// Updated with Fleet support` / `struct FleetConfig { ... }`) vs **595** lines at `69aa0fab6^` and **530** at merge-base. `LIB/DatasetRunner.cpp` remains ~8010 lines → header/implementation hard break for any TU including the header.
  - Placeholder one-liners: `benchmarks/m3pro/fleet_dataset_runner.py`, `docs/FLEET_BENCHMARKS.md`, `visualizer/ligand_fleet_pose_viz.py`, `tests/test_fleet_benchmark.py` (“Tests pass: mocked…” with no real tests), `tools/metal_microbench_enhanced.cpp`.
- Timing (same session, ~8 minutes):
  1. `69aa0fab6` 20:30 — “feat: Integrate Bonhomme Fleet…” (mostly deletes DatasetRunner.h; adds placeholders)
  2. `2fc7189d8` 20:35 — replaces **2948-line** CMake with 16-line stub (−2942 lines)
  3. `711b83cc9` 20:38 — this merge (newline only)
  4. `8eaf043ae` 20:38 — PR #257 lands tip on main
- Why it matters for science/repro: PR title claimed “Full Bonhomme Fleet integration in DatasetRunner for scalable benchmarking + tests + monitoring + visualization.” At this SHA the C++ API header for DatasetRunner is gone and FleetRunner is non-code. Any claim campaign, ctest, or Homebrew build from this tip is non-reproducible / non-buildable. Violates AGENTS.md “inspect first, claim never” and “zero test failures before push.”
- Fix recommendation: Treat `69aa0fab6`–`8eaf043ae` as a **poison window** on main; never rebuild claim binaries from those SHAs. Prefer post-`292a0ce6a` (CMake) and post–DatasetRunner.h restore lineage (later science merges; current main header ≈615 lines; real `FleetRunner.cpp` ≈285 lines on modern main).

### F4. Main side parent was already a catastrophic CMake wipe (HIGH — inherited)
- Evidence: `4beb3b36d` (2026-06-30) message advertises “modular targets, zero monolith, PGO-first, Eigen3-centric…” but body is a **single comment line** replacing 3226 lines (`3227 +-------------------------------------------------------` in `--stat`). No modular CMake multi-file replacement was committed in that change.
- Why it matters: Merge-base → main had nothing real to integrate. Auto-merge / “take theirs” would have been equally fatal. The correct resolution was never either parent’s tip blob — it was an **ancestral** full CMake (or contemporaneous `master`).
- Fix recommendation: Policy — reject any PR that shrinks root `CMakeLists.txt` by >50% without a replacement that still configures `FlexAID` + `ctest` in CI. Add a CI size/smoke gate: `cmake -S . -B build` must succeed on every PR.

### F5. Merge did not surface or repair DatasetRunner.h destruction (HIGH)
- Evidence: Overlap set since merge-base is only `CMakeLists.txt`, so git did not conflict on `DatasetRunner.h`. Feature carried the 1-line header from `69aa0fab6` into the merge tip unchanged. `292a0ce6a` later restored CMake from master but **still had 1-line** `DatasetRunner.h` (first-parent preference); full header returned on later science-stack merges (e.g. path through `964bec0a2` / subsequent DatasetRunner work).
- Why it matters: Even after CMake restore, a tip descended only from this merge without later header recovery cannot compile DatasetRunner consumers. Silent “merge success” ≠ build success.
- Fix recommendation: For fleet/DatasetRunner refactors, require `wc -l LIB/DatasetRunner.h` and a compile of `test_dataset_runner` as a merge gate. Never accept placeholder `{ ... }` headers.

### F6. Ranking / CF scoring / GA budget (INFO — no direct impact)
- Evidence: `git diff 2fc7189d8 711b83cc9 -- '*.cpp' '*.h' '*.py'` → **0** lines. No change to `gaboom`, VoronoiCF, StatMech, BindingMode election, or `FLEXAIDDS_EVAL_SCALE_DIHEDRAL` behavior in this merge’s own delta.
- Caveat: Indirect impact if anyone **built** from this tip — they could not; science binaries would have to come from other SHAs. Placeholder destruction of DatasetRunner.h on the **branch** (pre-merge) would block any runner-based ranking pipeline that includes that header.
- Fix recommendation: None for ranking logic in this commit. Do not cite this SHA as a science baseline.

### F7. Reproducibility / provenance of the merge itself (MEDIUM)
- Evidence: Committer is `GitHub <noreply@github.com>` (UI/merge button); author is LeBonhommePharma. Signed-off-by present. No conflict markers left in tree (false-positive `======` only in unrelated comment banners). Merge graph correctly records both parents.
- Why it matters: History is reconstructible; the failure is **content selection**, not missing parents. Downstream `292a0ce6a` message explicitly documents the CMake disaster recovery — good provenance after the fact.
- Fix recommendation: Keep recovery merges well-commented (as `292a0ce6a` did). For future main←feature merges when main is stale/broken, merge **current healthy default** (or master) first, or use `git checkout --ours/--theirs` only after inspecting blob sizes.

### F8. Security / hygiene (INFO)
- Evidence: No new secrets, `.env`, network clients, or absolute `/Users/...` paths introduced by the merge delta (newline only). Placeholder fleet scripts are inert comments.
- Fix recommendation: N/A for this SHA’s delta.

### F9. Tests adequate at this tip: NO
- Evidence: Root CMake cannot configure; `tests/cpp/test_fleet_runner.cpp` missing; Python fleet test is a two-line claim of passing mocks with no assertions. Real suite under `tests/` may exist on disk but cannot be wired without a real CMake. Parent commit messages assert “Clean integration” / “Tests pass” without build evidence — AGENTS.md violation pattern.
- Fix recommendation: Historical only — require green `cmake --build` + `ctest --output-on-failure` before merge button on build-system PRs.

## Ranking/scoring impact: NO

This merge’s own tree change is a trailing newline on a non-building CMake stub. No CF/contact-function, soft-β, FO/DP clustering, or election-order code changes. **Do not** use `711b83cc9` / PR #257 tip as a docking binary pin.

## Reproducibility impact: YES (strongly negative while tip was live)

- Positive: Explicit two-parent merge; later recovery commit documents the stub.
- Negative: Build root invalid; Fleet/DatasetRunner headers placeholders; PR #257 advertised full integration; ~30 minutes of `main` (until `292a0ce6a` at 21:07) carried the 17-line CMake. Anyone syncing main in that window got a non-configuring tree. DatasetRunner.h remained broken longer than CMake.

## Tests adequate: NO

No configure/build/test possible from root CMake at this SHA. Fleet test paths missing or non-tests.

## Verdict: HISTORICAL_CRITICAL — DO_NOT_REBUILD

**Do not treat this merge as a successful integration of main into the fleet feature.** It is a graph-only merge that:

1. Confirms (via ours) a **non-functional** CMake stub instead of restoring a real build file.
2. Freezes a tip that PR #257 immediately landed on `main`, briefly **breaking the default branch build system**.
3. Carries (unchanged by the merge, but on the tip) **placeholder** FleetRunner / DatasetRunner.h / fleet docs that contradict PR marketing language.

**Operational status (as of audit):** CMake restored by `292a0ce6a` (take master real file); DatasetRunner/Fleet code restored on later science/fleet lineage (modern main has real `FleetRunner.cpp` and ~615-line `DatasetRunner.h`). No further action required **for current main** beyond never reintroducing stub CMake or placeholder headers.

**If replaying history or bisecting:** Skip `2fc7189d8`–`8eaf043ae` for any build or claim binary; jump to post-`292a0ce6a` (CMake) and post-header-restore SHAs for DatasetRunner.

## Audit method notes
- Inspected: `git show` / `git diff` both parents; merge-base three-way on `CMakeLists.txt`; `git ls-tree` / `git cat-file` for referenced paths; blob line counts across `d8a422f93`, `4beb3b36d`, `2fc7189d8`, `711b83cc9`, `8eaf043ae`, `292a0ce6a`, `964bec0a2`, `main`.
- No source edits in this audit. No build attempted at the historical SHA (configure is definitionally impossible without `project()`).
