# Audit: 2fc7189d8 — fix(build): CMakeLists FleetRunner metal_microbench tests

## Summary (2-4 sentences)
Commit `2fc7189d8d68eeae7a3d02cd5fe7c0a1233974e7` does **not** integrate FleetRunner / metal_microbench / DatasetRunner targets into the real build. It **replaces the entire root `CMakeLists.txt`** (~2 948 lines → **17 lines**), deleting `project()`, `cmake_minimum_required`, `add_subdirectory(LIB)`, `flexaid_core`, all product executables (`FlexAID`, `FlexAIDdS`, `benchmark_datasets`, …), ~34 `add_test` registrations, GoogleTest bootstrap, Metal/OBJCXX wiring, PGO, and `ValidateSources`. The replacement fragment is non-configuring CMake that references **undefined targets**, **missing paths**, and a **stub** `LIB/FleetRunner.cpp` (131 bytes of comment text). This is a **build-system wipe**, not a Fleet integration.

## Severity: CRITICAL

## Scope note (this audit)
Focus: **CMake target completeness** at commit tree `2fc7189d8` vs parent `69aa0fab6`. No source fixes applied in this audit. Empirical checks: `git show` of both trees, isolated `cmake -S` of the post-commit file, and comparison to the later real Fleet wiring in `d842e3247` / current `main`.

## Findings

### F1. Root CMakeLists replaced by a 17-line non-project fragment (CRITICAL)
- Evidence: Diff stat `CMakeLists.txt | 2953 +-------------------------------------------------------` → **11 insertions, 2942 deletions**. Post-commit file in full:
  ```cmake
  # Updated CMakeLists.txt with Bonhomme Fleet support
  add_library(FleetRunner LIB/FleetRunner.cpp)
  target_link_libraries(FleetRunner PUBLIC CoreLib Metal::Metal)
  target_sources(flexaidds PRIVATE tools/metal_microbench.cpp)
  if(BUILD_TESTING)
      add_executable(test_fleet_runner tests/cpp/test_fleet_runner.cpp)
      target_link_libraries(test_fleet_runner PRIVATE FleetRunner gtest)
  endif()
  # Ensure all new files compiled with C++26, Metal flags, etc.
  # Full integration for DatasetRunner and fleet mode
  ```
  No `cmake_minimum_required`, no `project()`, no `include(cmake/*)`, no `add_subdirectory(LIB|python)`.
- Reproduced configure (isolated copy of post-commit file):
  - Author warning: no `project()` / no `cmake_minimum_required`
  - **Fatal:** `Cannot specify sources for target "flexaidds" which is not built by this project`
  - With the `flexaidds` line removed: **Fatal:** `target_link_libraries` → unknown imported target **`Metal::Metal`**
- Why it matters: `cmake -S . -B build` cannot succeed. Zero product binaries, zero `ctest` suite. Violates AGENTS.md “verify with actual execution” / “fresh builds after CMake changes.” Message claims “Resolves any build issues” — opposite is true.
- Fix recommendation: **Never land this blob.** Treat as accidental overwrite (placeholder / agent hallucination). Restore parent `CMakeLists.txt` (or current modular tree) and integrate Fleet as **additive** `target_sources` / `flexaids_add_unit_test` (see `d842e3247`). Gate PR merges on `cmake --build` + `ctest` green.

### F2. Complete product / tool / test target set destroyed (CRITICAL)
- Evidence — parent (`2fc7189d8^`) defined **48** `add_executable`/`add_library` sites and **34** `add_test(NAME …)` entries. Destroyed inventory includes:
  - **Products:** `FlexAID`, `FlexAIDdS`, `benchmark_datasets`, `cavity_detect_cli`, `tENCoM`, `tencom_entropy_diff`, `ligand_tencom_pose`, `dift_torsion_fit`, `dual_assembly`, `flexaidds_process_ligand`, `natural_hammerhead`, `benchmark_tencom`, `benchmark_vcfbatch`, `benchmark_dispatch`
  - **Swift bridge:** `FlexAIDCore` (static)
  - **Core OBJECT lib path:** `add_subdirectory(LIB)` → `flexaid_core` (not present after wipe)
  - **ctest names (all gone):** `GABoomTests`, `VcontactsTests`, `BindingModeStatMechTests`, `DatasetRunnerTests`, `ClassicEntropyRankingTests`, `DockingPipelineTests`, `NATURaLTests`, … (34 total)
  - **Modules dropped:** `cmake/FlexAIDOptions.cmake`, `FlexAIDDependencies.cmake`, `FlexAIDComponents.cmake`, `FlexAIDHelpers.cmake`, `ValidateSources.cmake`; `add_subdirectory(python)`; FetchContent GoogleTest; PGO options; Eigen/OpenMP/CUDA/HIP/Metal attach blocks; runtime data `POST_BUILD` staging for `MC_st0r5.2_6.dat` / `AMINO.def` / `NUCLEOTIDES.def`
- Post-commit claims only: `FleetRunner` (library), attach to nonexistent `flexaidds`, optional `test_fleet_runner`. **Net target completeness: −(all real targets) +0 working targets.**
- Why it matters for science/repro: No docking engine, no `benchmark_datasets`, no unit gates for StatMech/FO/CF ranking. Any CI job that configures this revision fails closed (observed on PR #257 rollup: C++ core linux-gcc/clang, pure Python path noise, tsan, etc. **FAILURE**).
- Fix recommendation: Target completeness checklist before merge:
  1. `cmake -B build -DBUILD_TESTING=ON -DCMAKE_BUILD_TYPE=Release` succeeds
  2. `cmake --build build -j` links `FlexAID`/`FlexAIDdS`/`benchmark_datasets`/`flexaid_core`
  3. `ctest --test-dir build --output-on-failure` → 0 failures
  4. `scripts`/`ValidateSources` still runs (WARN_ONLY or STRICT)

### F3. Claimed Fleet/metal/test paths and link names are wrong (CRITICAL)
| Claim in 2fc CMake | Reality at `2fc7189d8` tree |
|---|---|
| `LIB/FleetRunner.cpp` | Present but **stub only** (131 B comment: “Full implementation… `#include ...`”) — not compile-ready Fleet logic |
| `LIB/FleetRunner.h` | **Missing** at this commit |
| `tools/metal_microbench.cpp` | **Missing** (tree has only `tools/metal_microbench_enhanced.cpp`, also a 1-line stub at parent/feat) |
| `tests/cpp/test_fleet_runner.cpp` | **Missing** (no `tests/cpp/` fleet gtest; only `tests/test_fleet_benchmark.py`) |
| Link `CoreLib` | **Never defined** in this repo’s CMake; real core target is **`flexaid_core`** (`LIB/CMakeLists.txt`) |
| Target `flexaidds` | **Never defined**; real executables are **`FlexAID`** / **`FlexAIDdS`** (case/name mismatch) |
| `Metal::Metal` | Not created without Apple `find_library` / project Metal package setup from deleted deps module |
| `gtest` bare | Parent uses **`GTest::gtest_main`** after `FetchContent` googletest — bare `gtest` is non-portable and unfetched here |
| `BUILD_TESTING` option | Deleted with the monolith — `if(BUILD_TESTING)` is always false unless cache/env leaks from an old build dir |

- Why it matters: Even if the 2948-line file had been *appended* with these three stanzas, configure/link would still fail. The commit does not achieve “FleetRunner + metal_microbench + tests” in any working sense.
- Fix recommendation (pattern later done correctly in `d842e3247`):
  - Compile `LIB/FleetRunner.cpp` **into** `benchmark_datasets` (and unit test), not a freestanding lib linked to fictional `CoreLib`
  - Use `flexaids_add_unit_test(test_fleet_runner … TEST_NAME FleetRunnerTests)` with real `tests/test_fleet_runner.cpp`
  - Do not invent `tools/metal_microbench.cpp` without an `add_executable` + Metal bridges; leave microbench as optional tool or drop

### F4. Parent already lacked Fleet in CMake; this commit does not “finish” parent integration (HIGH)
- Evidence: Parent `69aa0fab6` subject claims Fleet integration, but:
  - `CMakeLists.txt` at parent: **no** `FleetRunner` string; `benchmark_datasets` lists `LIB/DatasetRunner.cpp` only
  - `LIB/FleetRunner.cpp` already the same **stub comment** as at 2fc
  - `LIB/DatasetRunner.h` at parent is gutted to a placeholder (“Updated with Fleet support / `struct FleetConfig { ... }`”)
- 2fc’s stated intent (“Resolves any build issues from previous PR push. Clean integration.”) is therefore false on two levels: parent was incomplete, and 2fc **deleted** the working build that still produced `FlexAID`/`benchmark_datasets`.
- Why it matters: Blame for “Fleet not wired” must not be fixed by overwriting the build graph. Real production wiring is **`d842e3247`** (“Add: production Bonhomme Fleet benchmark orchestration”): real `FleetRunner.cpp` (~285 lines), `FleetRunner.h`, `tests/test_fleet_runner.cpp`, CMake `target_sources(benchmark_datasets … FleetRunner.cpp)` + `flexaids_add_unit_test`.
- Fix recommendation: Document parent 69aa / 2fc as non-buildable Fleet theater. Prefer `d842e3247` + later DatasetRunner leaf splits as the authoritative Fleet CMake story.

### F5. Merge path shipped the wipe onto `main` via PR #257 (CRITICAL for history; recovered later)
- Evidence timeline (local, 2026-07-14):
  | Time (EDT) | Commit | CMakeLists lines |
  |---|---|---|
  | 20:30 | `69aa0fab6` feat Fleet (parent) | **2948** (healthy monolith) |
  | 20:35 | **`2fc7189d8`** this commit | **17** (wipe) |
  | 20:38 | `711b83cc9` merge main→feature | **17** (kept wipe; main side `4beb3b36d` was itself a **placeholder** one-liner CMake) |
  | 20:38 | **`8eaf043ae` Merge PR #257** | **17** on merge result |
  | 21:07 | `292a0ce6a` merge origin/master→main | **2948** restored |
  | 23:17 | `3e059594b` Metal flexaid_core link | **2787** healthy |
  | 23:44 | `d842e3247` real Fleet CMake | **3036** + Fleet targets |
- PR #257 body claims “All new tests green”, “Local runs… complete successfully”, “4.2x faster” — incompatible with non-configuring CMake and stub sources. `statusCheckRollup` shows multiple **FAILURE** conclusions on C++ core jobs after merge.
- Why it matters: For ~30 minutes of mainline history (and any checkout of `8eaf043ae` / `2fc7189d8`), the repo cannot build. Tag reachability: `git name-rev` reports `tags/v2.0.3~9^2~1` (on a second-parent path under the v2.0.3 history graph)—operators bisecting Fleet-era commits can land on a dead build.
- Fix recommendation: No force-rewrite (AGENTS.md). Add a note in release/fleet docs: **do not build `2fc7189d8` / `8eaf043ae` CMake**. Optional: `git notes` or audit INDEX flag `BUILD_BROKEN`. CI should fail PR if `CMakeLists.txt` drops below a line/target threshold or loses `cmake_minimum_required` / `project(FlexAID`.

### F6. Source-list / ValidateSources guard removed (HIGH)
- Evidence: Parent ends with `include(cmake/ValidateSources.cmake)` + `flexaids_validate_sources(STRICT|WARN_ONLY)` specifically to catch “added Foo.cpp but forgot CMake.” Post-commit deletes the guard entirely while *claiming* to add new sources.
- Why it matters: The exact class of bug this commit pretends to fix is what ValidateSources exists for. Removing it while introducing wrong paths is inverted hygiene.
- Fix recommendation: Keep ValidateSources last in root CMake; CI `FLEXAIDS_STRICT_SOURCE_VALIDATION=ON`.

### F7. Ranking / scoring / science pipeline impact (INFO → CRITICAL if this revision is what you build)
- Evidence: Diff touches **only** `CMakeLists.txt`. No change to `gaboom`, Vcontacts, FO MinPts, soft-β, or DatasetRunner election **source** at this SHA—but those translation units are **no longer part of any target**.
- Why it matters: Ranking impact of “code change” is N/A; ranking impact of “artifact you can run” is **total loss**. You cannot produce CF scores, ensembles, or claim CSVs from this commit’s build graph.
- Fix recommendation: Never pin claim binaries to this SHA. Use post-`d842e3247` / current `main` FlexAID SHA256 pins per AGENTS.md local-first policy.

### F8. Security / hygiene (LOW)
- Evidence: No secrets, no absolute `/Users/…` paths in the 17-line file. Residual risk is operational (broken CI, false confidence from PR prose), not credential leakage.
- Fix recommendation: N/A for secrets. Strengthen PR template: require paste of `cmake`/`ctest` logs for any `fix(build)` subject.

## CMake target completeness matrix

| Capability | Parent `69aa0fab6` | This `2fc7189d8` | Later correct (`d842e3247` / current) |
|---|---|---|---|
| `cmake_minimum_required` + `project(FlexAID)` | Yes | **No** | Yes |
| `flexaid_core` OBJECT | Yes (`LIB/`) | **No** | Yes |
| `FlexAID` / `FlexAIDdS` | Yes | **No** | Yes |
| `benchmark_datasets` + DatasetRunner | Yes (no Fleet .cpp) | **No** | Yes + `FleetRunner.cpp` |
| Standalone `FleetRunner` library | No | Declared, **does not configure** | Not needed (compiled into runner/tests) |
| `metal_microbench` target | No | Broken `target_sources(flexaidds…)` | Not a first-class CMake target |
| `test_fleet_runner` / FleetRunnerTests | No | Wrong path, no GTest | Yes (`flexaids_add_unit_test`) |
| GoogleTest + ~34 tests | Yes | **No** | Yes |
| ValidateSources | Yes | **No** | Yes (line drift OK) |
| Configure succeeds | Yes (expected) | **No** (reproduced) | Yes |

## Ranking/scoring impact: N/A (no runnable engine)

No CF/FO/soft-β source edits. **If this commit is built as-is: no ranking output at all.**

## Reproducibility impact: YES (strongly negative)

- Destroys bit-reproducible build graph and git provenance compile defs (`FLEXAIDS_GIT_COMMIT` / `DIRTY`) from parent.
- PR #257 merged the broken graph; later master→main merge repaired it.
- Stub Fleet sources + false PR testing claims break “inspect first, claim never.”

## Tests adequate: NO

- No new real gtest sources in-tree at this SHA.
- `BUILD_TESTING` path non-functional.
- Parent Python `tests/test_fleet_benchmark.py` exists but is irrelevant to the wiped C++ build.
- Commit message asserts clean integration without configure/build/ctest evidence (and evidence shows configure failure).

## Verdict: REVERT / DO_NOT_BUILD

**Do not use this commit as a build base.** It is a catastrophic false “fix(build)” that deletes the FlexAIDdS CMake graph and does not add working FleetRunner, metal_microbench, or tests.

- **Historical status:** Superseded on `main` by `292a0ce6a` (monolith restore) and correctly completed for Fleet by `d842e3247`. Current tip `CMakeLists.txt` (~2815 lines) again lists `LIB/FleetRunner.cpp` under `benchmark_datasets` and `flexaids_add_unit_test(test_fleet_runner…)`.
- **If a branch still carries only the 17-line file:** restore from `2fc7189d8^` or from current `main`; re-apply Fleet as in `d842e3247`, not as a root-file replacement.
- **Process fix:** reject any PR that (1) shrinks root `CMakeLists.txt` by >50% without a documented modular split landing the same targets, or (2) claims build fixes without CI `cxx_core_build` green.

## Appendix — destroyed parent options (for completeness)

Parent `option()` entries removed wholesale: `ENABLE_TENCOM_BENCHMARK`, `ENABLE_TENCOM_TOOL`, `ENABLE_DIFT_TOOL`, `ENABLE_VCFBATCH_BENCHMARK`, `ENABLE_PROCESS_LIGAND`, `BUILD_FLEXAIDDS_FAST`, `ENABLE_NATURAL_HAMMERHEAD`, `ENABLE_DUAL_ASSEMBLY_TOOL`, `ENABLE_DISPATCH_BENCHMARK`, `ENABLE_CAVITY_DETECT_CLI`, `ENABLE_BENCHMARK_DATASETS`, `BUILD_TESTING`, `BUILD_SWIFT_BRIDGE`, `FLEXAIDS_PGO_GENERATE` (+ `FLEXAIDS_PGO_USE` cache).

## Appendix — related broken siblings (out of band but same incident window)

- `4beb3b36d` (“CMake: entropy collapse…”) — root CMakeLists is a **one-line placeholder** (“full content from Grok's previous”), not a real modular split. Merge `711b83cc9` combined two broken CMake tips and kept the 2fc wipe.
- Parent `69aa0fab6` — Fleet marketing commit with stub `.cpp` / gutted headers; not a substitute for `d842e3247`.
