#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
REPO_ROOT="$(cd "$ROOT/../../.." && pwd)"
exec python3 "$ROOT/scripts/validate_dataset_semantics.py" --path "$REPO_ROOT/benchmarks/datasets" "$@"
